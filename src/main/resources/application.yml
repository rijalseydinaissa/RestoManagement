spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestiona_app
    password: postgres
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa :
    hibernate:
      ddl-auto: update
server:
  port: 8081
